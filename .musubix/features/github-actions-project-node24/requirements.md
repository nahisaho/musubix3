---
schemaVersion: 1
feature: github-actions-project-node24
---
# GitHub Actions project Node.js 24

Source: User instruction to use Node.js 24 for GitHub Actions.

## REQ-GITHUB-ACTIONS-PROJECT-NODE24-001: Use Node.js 24 for primary workflow execution
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The repository shall execute primary CI validation, release preparation, attestation, and npm publication with Node.js 24.
Acceptance: `.github/workflows/ci.yml` configures `core-portability` with `name: Core (${{ matrix.os }}, Node 24)` and exactly one setup-node `node-version: 24`; `.github/workflows/release.yml` configures exactly three setup-node steps with `node-version: 24`; `.github/workflows/npm-publish.yml` configures exactly one setup-node step with `node-version: 24`; the `node-compatibility` matrix remains exactly `[20, 24]` so the declared `engines.node >=20` compatibility floor and Node.js 24 are both tested; `.github/workflows/ci.yml` jobs `native-adapters` and `formal-solvers`, and `.github/workflows/dependency-audit.yml` job `dependency-audit`, remain on Node.js 24; the CI-contract code in `scripts/check-npm-audit-remediation.mjs` carries `@implements REQ-GITHUB-ACTIONS-PROJECT-NODE24-001`, requires the full test command on Node.js 24 for `core-portability`, and retains the `[20, 24]` compatibility matrix and focused-job commands; `TEST-NPM-AUDIT-REMEDIATION-003` carries `@verifies REQ-GITHUB-ACTIONS-PROJECT-NODE24-001`, scopes its altered-version and duplicate-setup-node mutations to the `core-portability` job block rather than a bare first `node-version: 24` occurrence, and continues to require `CI_CONTRACT`; `CODE-GITHUB-ACTIONS-NODE24-RUNTIME-002` and `TEST-GITHUB-ACTIONS-PROJECT-NODE24-001` also carry REQ-GITHUB-ACTIONS-PROJECT-NODE24-001 and positively require all four current publishing setup-node values to normalize with `String(...)` to `24`; no workflow trigger, permission, environment, concurrency rule, action pin, action input other than the enumerated `node-version` values, artifact hand-off, command, OIDC/provenance behavior, or publication control changes.

## REQ-GITHUB-ACTIONS-PROJECT-NODE24-002: Preserve reviewed workflow correspondence
Priority: must
Type: non-functional
Pattern: ubiquitous
Statement: The repository shall prove deterministically that the Node.js 24 workflow migration changes only reviewed runtime literals and preserves immutable pre-change publishing workflow provenance.
Acceptance: In addition to the action SHA, version-comment, and reviewed input-table differences already permitted by REQ-GITHUB-ACTIONS-NODE24-RUNTIME-002, comparison with `.github/workflows/ci.yml` in `tests/fixtures/github-actions-node24-runtime/baseline.json` allows only `jobs.core-portability.name` to transition from `Core (${{ matrix.os }}, Node 22)` to `Core (${{ matrix.os }}, Node 24)` and that job's setup-node `with.node-version` to transition from `22` to `24`, positively requires both current values to be Node.js 24, and keeps every other new CI difference gate-blocking; CHANGE-0021's input-change table remains a zero-data-row table and is not used for this migration; `tests/fixtures/release-asset-publishing/workflow-baseline.json` stores exact `priorSource` bytes including the final newline, `priorSourceSha256`, `priorCommit`, and `priorBlobId` for commit `46edcde9209cc2708c5d6669c98af9e55aa7c7cb`: release blob `a0013a4f5530c84f812242664523ea965dd67306` and SHA-256 `30aaa213cf5ed8158e374f5e9b71ac8dd3a7eb1c0551d5e274b9e6f8b3183f6a`, npm-publish blob `ccb30e3b980a93fa9c323fb81944e5253257f2d7` and SHA-256 `4ccb354eb54c11ac9e8bf9b7d7925aad95d78292f985cad5e57367108733e616`; both fixture paths use the copied file beneath `GITHUB_ACTIONS_WORKFLOW_ROOT` when it exists and otherwise fall back to the repository fixture, so existing workflow-only isolated roots retain diagnostics while `TEST-GITHUB-ACTIONS-PROJECT-NODE24-002` can copy and mutate both fixtures without editing repository files; the fixture changes `approvedChange` from `CHANGE-0023` to `CHANGE-0047` and records each new current `sourceSha256`; `CODE-GITHUB-ACTIONS-NODE24-RUNTIME-002` carries `@implements REQ-GITHUB-ACTIONS-PROJECT-NODE24-002`, and `TEST-GITHUB-ACTIONS-PROJECT-NODE24-002` carries `@verifies REQ-GITHUB-ACTIONS-PROJECT-NODE24-002`; `scripts/check-github-actions.mjs` embeds checker-side literals for the commit, both prior blob IDs, and both prior SHA-256 values, compares fixture `priorCommit`, `priorBlobId`, and `priorSourceSha256` to those literals, updates the checker-side approved-change literal to `CHANGE-0047`, recomputes each Git blob ID and SHA-256 from `priorSource` without repository-history access, and compares fixture `sourceSha256` only to the recomputed current workflow digest; it requires equal prior/current line counts with exactly three differing release lines and one differing npm-publish line, each matching the same indentation plus `node-version: 22` to `node-version: 24`, while every other line remains byte-identical; deterministic tests reject a baseline refresh, provenance mismatch, missing final newline, unreviewed source difference, or any non-enumerated protected workflow change.

## REQ-GITHUB-ACTIONS-PROJECT-NODE24-003: Document and retain operational evidence
Priority: must
Type: non-functional
Pattern: ubiquitous
Statement: The repository shall document the Node.js 24 CI policy in English and Japanese and retain explicit operational evidence states for the migrated workflows.
Acceptance: The two core-CI descriptions and the action-runtime-versus-tested-versions sentence in each of `README.md` and `README-ja.md` state that core portability runs Node.js 24 on Linux, Windows, and macOS runners, enumerate the package-test versions as exactly Node.js 20 and 24, state that action implementation runtime is independent of those package-test versions, and leave the documented `engines.node >=20` floor unchanged; exact hosted-runner label wording remains governed by REQ-GITHUB-ACTIONS-NODE24-RUNTIME-003; each document owns three distinct exact raw source lines, including leading whitespace, and reflow preserves the adjacent native-adapter/formal-solver and hosted-runner-policy prose; documentation failures use `PROJECT_NODE24_DOCUMENTATION` and identify the file plus failing statement; each README path uses the copied file beneath `GITHUB_ACTIONS_PROJECT_NODE24_DOC_ROOT` when that file exists and otherwise falls back to the repository file; `TEST-GITHUB-ACTIONS-PROJECT-NODE24-003` rejects drift in all six statements and the operational evidence grammar without mutating repository files; `CODE-GITHUB-ACTIONS-NODE24-RUNTIME-003` and `CODE-GITHUB-ACTIONS-PROJECT-NODE24-EVIDENCE-001` each carry `@implements REQ-GITHUB-ACTIONS-PROJECT-NODE24-003`; the existing CHANGE-0021 evidence contract required by REQ-GITHUB-ACTIONS-NODE24-RUNTIME-001 remains enforced unchanged and continues to use `GITHUB_ACTIONS_CHANGE_PATH` for its isolated fixtures; isolated `evidence` tests set both `GITHUB_ACTIONS_CHANGE_PATH` and `GITHUB_ACTIONS_PROJECT_NODE24_CHANGE_PATH` to independent valid fixture documents so neither contract reads production state; the checker reads production path `.musubix/changes/CHANGE-0047.md`, and only the exact path override `GITHUB_ACTIONS_PROJECT_NODE24_CHANGE_PATH` may replace it in isolated tests; it requires exactly one of each anchor `Post-merge CI run:`, `Core ubuntu-latest conclusion:`, `Core windows-latest conclusion:`, `Core macos-latest conclusion:`, `Deferred release evidence:`, and `Deferred npm-publish evidence:`; failures use diagnostic code `CHANGE_PROJECT_NODE24_EVIDENCE` and identify CHANGE-0047 plus the failing anchor; for any front matter status other than `completed`, accepted pending values are exactly `pending until the first merged CI execution.`, `pending.` for each core conclusion, `pending until the next natural release execution.`, and `pending until the next natural publication.`, or the completed grammar; a completed post-merge run must match `https://github.com/nahisaho/musubix3/actions/runs/<digits>`, each completed core conclusion value must be exactly `success`, and each completed deferred release or npm-publish value must match `https://github.com/nahisaho/musubix3/actions/runs/<digits>; Node.js 20 warning: absent`; when status is `completed`, a pending post-merge anchor, malformed run URL, or non-`success` core conclusion is diagnostic; release and npm-publish anchors may retain their exact deferred pending values until those workflows next execute naturally, and no release or publication is triggered solely to manufacture evidence.
